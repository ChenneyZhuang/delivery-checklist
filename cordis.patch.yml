# delivery-checklist bundle patch: registers the skill with DSH.
# Ships SKILL.md only — no host-process code, no browser client.
- insert:
    - id: delivery-checklist
      name: 'delivery-checklist'
